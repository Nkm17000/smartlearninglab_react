// Backward-compatible entry point.
// The old PDF import form had its own free-text category field. Keep the route
// safe by using the same upload-level taxonomy workflow as Bulk Content Studio.
export {default} from './AdminBulkContentScreen';
