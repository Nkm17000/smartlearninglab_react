# Final FE update - Quiz + Mock Test

Changes:
- Added `Mock Test` as the last student top-navigation item.
- Renamed the adaptive-test experience to `Mock Test`.
- Mock Test has a real Submit Mock Test action and result screen.
- Mock Test uses a server-side test session so results are graded correctly.
- Fixed student quiz navigation and standalone quiz back navigation.
- Fixed quiz loading flow and supports bulk-imported quizzes after admin publish.
- Admin `quizzes` route now correctly opens `AdminQuizzesScreen`.

Run:
`npm install`
`npx expo start --web -c`
